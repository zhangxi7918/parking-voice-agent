[project]
name = "parking-voice-agent"
version = "0.1.0"
description = "Parking gatekeeper voice agent backend."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "livekit-agents[elevenlabs,openai,silero,turn-detector]>=1.5,<2",
  "livekit-api>=1.0,<2",
  "uvicorn[standard]>=0.30.0",
  "httpx>=0.27.0",
  "pydantic>=2.7.0",
  "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
  "ruff>=0.5.0",
]

[project.scripts]
voice-agent-dev = "voice_agent.dev:main"

[build-system]
requires = ["setuptools>=70.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
